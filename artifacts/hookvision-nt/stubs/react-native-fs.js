// Stub for react-native-fs — only the bundle_resource_io path in
// @tensorflow/tfjs-react-native needs this at import time. We never call
// those bundle-loading helpers (CV runs server-side), so a no-op stub is safe.
module.exports = {};
